import { BloodDonationCamp } from './blood-donation-camp';

describe('BloodDonationCamp', () => {
  it('should create an instance', () => {
    expect(new BloodDonationCamp()).toBeTruthy();
  });
});
